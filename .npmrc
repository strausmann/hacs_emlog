# NPM Configuration for strict security
# Note: 6 low-severity vulnerabilities exist in dev-dependencies (commitizen chain)
# These are in build/commit tools only, not in runtime code
# See: https://github.com/advisories/GHSA-52f5-9888-hmc6

# Audit level: audit reports low severity and above
audit-level=low

# Don't fail builds on audit warnings (dev-only vulnerabilities)
# Production code has no security vulnerabilities
legacy-peer-deps=false
